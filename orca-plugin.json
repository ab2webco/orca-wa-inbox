{
  "manifestVersion": 1,
  "id": "orca-wa-inbox",
  "publisher": "ab2web",
  "name": "WA Inbox",
  "version": "4.0.0",
  "description": "Orca Lab's official WhatsApp plugin, built by Ab2Web. Pair your line with a QR code, turn client conversations into tracked work, and decide per conversation how far your agent may act.",
  "engines": {
    "orca": ">=1.4.0"
  },
  "pluginApi": 1,
  "main": "main.mjs",
  "contributes": {
    "panels": [
      {
        "id": "wa-inbox-activity",
        "title": "WhatsApp",
        "icon": "icons/whatsapp.svg",
        "entry": "activity.html",
        "surface": "nav"
      },
      {
        "id": "wa-inbox-config",
        "title": "WhatsApp Inbox",
        "icon": "icons/whatsapp.svg",
        "entry": "config.html",
        "surface": "settings"
      }
    ],
    "commands": [
      {
        "id": "wa-inbox.sync",
        "title": "WhatsApp: Refresh conversations"
      },
      {
        "id": "wa-inbox.scope.list",
        "title": "WhatsApp: View scope"
      },
      {
        "id": "wa-inbox.scope.set",
        "title": "WhatsApp: Map conversation"
      },
      {
        "id": "wa-inbox.inbox",
        "title": "WhatsApp: Messages addressed to me"
      },
      {
        "id": "wa-inbox.settings",
        "title": "WhatsApp: Agent settings"
      },
      {
        "id": "wa-inbox.doctor",
        "title": "WhatsApp: Check requirements"
      }
    ],
    "events": [
      {
        "on": "agent.status.changed"
      }
    ],
    "automations": [
      {
        "id": "triage",
        "title": "WhatsApp: triage",
        "provider": "claude",
        "trigger": "*/5 8-18 * * 1-5",
        "timezone": "America/Bogota",
        "precheck": "WA=$(python3 -c \"import json, os, sys\nLLAVE = 'ab2web.orca-wa-inbox'\nPERFILES = ('profiles/local-default/orca-data.json', 'orca-data.json')\ndef version(raiz):\n    try:\n        with open(os.path.join(raiz, 'orca-plugin.json')) as fh:\n            crudo = json.load(fh).get('version') or ''\n    except Exception:\n        return None\n    partes = []\n    for pedazo in str(crudo).replace('-', '.').split('.'):\n        if not pedazo.isdigit():\n            break\n        partes.append(int(pedazo))\n    return tuple(partes + [0, 0, 0])[:3] if partes else None\ndef candidato(raiz):\n    b = os.path.join(raiz, 'bin')\n    v = version(raiz)\n    if not v or not os.path.isdir(b):\n        return None\n    return (v, os.path.getmtime(b), b)\nbase = (os.path.expanduser('~/Library/Application Support') if sys.platform == 'darwin'\n        else os.environ.get('APPDATA') or os.path.expanduser('~/.config'))\ndev, instalados = [], []\nfor carpeta in (os.listdir(base) if os.path.isdir(base) else []):\n    raiz = os.path.join(base, carpeta)\n    p = os.path.join(raiz, 'plugins', LLAVE)\n    cur = os.path.join(p, 'current')\n    if os.path.isfile(cur):\n        c = candidato(os.path.join(p, open(cur).read().strip()))\n        if c:\n            instalados.append(c)\n    for perfil in PERFILES:\n        f = os.path.join(raiz, perfil)\n        if not os.path.isfile(f):\n            continue\n        try:\n            with open(f) as fh:\n                ajustes = json.load(fh).get('settings') or {}\n        except Exception:\n            continue\n        for ruta in ajustes.get('devPluginPaths') or []:\n            c = candidato(ruta)\n            if c:\n                dev.append(c)\nprint(max(dev or instalados)[2] if (dev or instalados) else '')\") && [ -x \"$WA/wa-scope\" ] || exit 1; \"$WA/wa-scope\" pending",
        "prompt": "prompts/triage.md",
        "workspace": "plugin-owned"
      },
      {
        "id": "take",
        "title": "WhatsApp: Take what was marked",
        "provider": "claude",
        "trigger": "*/2 * * * *",
        "timezone": "America/Bogota",
        "precheck": "WA=$(python3 -c \"import json, os, sys\nLLAVE = 'ab2web.orca-wa-inbox'\nPERFILES = ('profiles/local-default/orca-data.json', 'orca-data.json')\ndef version(raiz):\n    try:\n        with open(os.path.join(raiz, 'orca-plugin.json')) as fh:\n            crudo = json.load(fh).get('version') or ''\n    except Exception:\n        return None\n    partes = []\n    for pedazo in str(crudo).replace('-', '.').split('.'):\n        if not pedazo.isdigit():\n            break\n        partes.append(int(pedazo))\n    return tuple(partes + [0, 0, 0])[:3] if partes else None\ndef candidato(raiz):\n    b = os.path.join(raiz, 'bin')\n    v = version(raiz)\n    if not v or not os.path.isdir(b):\n        return None\n    return (v, os.path.getmtime(b), b)\nbase = (os.path.expanduser('~/Library/Application Support') if sys.platform == 'darwin'\n        else os.environ.get('APPDATA') or os.path.expanduser('~/.config'))\ndev, instalados = [], []\nfor carpeta in (os.listdir(base) if os.path.isdir(base) else []):\n    raiz = os.path.join(base, carpeta)\n    p = os.path.join(raiz, 'plugins', LLAVE)\n    cur = os.path.join(p, 'current')\n    if os.path.isfile(cur):\n        c = candidato(os.path.join(p, open(cur).read().strip()))\n        if c:\n            instalados.append(c)\n    for perfil in PERFILES:\n        f = os.path.join(raiz, perfil)\n        if not os.path.isfile(f):\n            continue\n        try:\n            with open(f) as fh:\n                ajustes = json.load(fh).get('settings') or {}\n        except Exception:\n            continue\n        for ruta in ajustes.get('devPluginPaths') or []:\n            c = candidato(ruta)\n            if c:\n                dev.append(c)\nprint(max(dev or instalados)[2] if (dev or instalados) else '')\") && [ -x \"$WA/wa-scope\" ] || exit 1; \"$WA/wa-scope\" pending --only-taken",
        "prompt": "prompts/take.md",
        "workspace": "plugin-owned"
      }
    ]
  },
  "capabilities": [
    {
      "kind": "process:spawn"
    },
    {
      "kind": "workspace:read"
    },
    {
      "kind": "notifications:show"
    },
    {
      "kind": "storage"
    },
    {
      "kind": "settings:own"
    },
    {
      "kind": "events:subscribe"
    }
  ]
}
