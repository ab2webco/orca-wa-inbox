{
  "name": "orca-wa-inbox",
  "private": true,
  "description": "Arnes de verificacion del plugin WhatsApp Inbox: paneles, prompts y CLIs.",
  "scripts": {
    "setup": "npm --prefix ../.orca-wa-inbox-deps install && ../.orca-wa-inbox-deps/node_modules/.bin/playwright install chromium-headless-shell",
    "check": "scripts/check-panels && scripts/check-prompts && scripts/check-harness && scripts/check-clis && scripts/check-closing && node test/manifest.test.mjs && node test/panels.test.mjs && node test/worker.test.mjs && npm run shots",
    "shots": "node test/shots.mjs"
  }
}
