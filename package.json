{
  "name": "orca-wa-inbox",
  "private": true,
  "description": "Arnes de verificacion del plugin WhatsApp Inbox: paneles, prompts y CLIs.",
  "scripts": {
    "setup": "npm --prefix ../.orca-wa-inbox-deps install",
    "check": "scripts/check-panels && scripts/check-prompts && scripts/check-clis && scripts/check-closing && node test/panels.test.mjs && node test/worker.test.mjs && npm run shots",
    "shots": "node test/shots.mjs"
  }
}
